ssh-keygen

git add *

git commit m "name"
 
 git push us Pass
 
 git branch branch name
 
 git chechout branch name - to switch branches (master)
 
 $ git checkout -b feature_branch_name
... edit files, add and commit ...
$ git push -u origin feature_branch_name